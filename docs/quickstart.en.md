# English quick start

1. Ask what the figure must demonstrate and inspect only the selected local data.
2. Run doctor; repair only the audited project-local Python dependencies when permitted.
3. Profile columns, types, missingness, units, and domain signals.
4. Recommend at most three charts and explain rejected alternatives and confirmation gates.
5. Show the palette selector and recommend two IDs based on group count, CVD, and grayscale output.
6. Confirm chart, mapping, axes, error semantics, order, transforms, palette, and one core claim.
7. Freeze a hash-bound RenderPlan; never hand-edit it.
8. Render only after the user confirms that licensed Origin starts manually.
9. Verify OPJU/PNG/PDF/TIF, source hash, object/font readback, and human visual QA.

Suggested prompt:

```text
Use $editaplot. Run doctor first, then inspect my data read-only. Recommend at most three
charts and explain column roles, units, errors, and low-confidence gates. Show the Chinese scientific
palette selector and recommend two palette IDs for my group count and grayscale output. Freeze the
plan only after I confirm. Do not modify source data or silently fit/calculate. Keep Origin editable
and require OPJU/PNG/PDF/TIF, object readback, and human visual QA.
```
